# {{ ansible_managed }}

concurrent = 1
check_interval = 0

[session_server]
  session_timeout = 1800

[[runners]]
  builds_dir = "{{ gitlab_runner_runners_builds_dir }}"
  cache_dir = "{{ gitlab_runner_runners_cache_dir }}"
  executor = "docker"
  name = "alvistack/gitlab-runner"
  token = "{{ gitlab_runner_runners_token }}"
  url = "https://gitlab.com/"
  [runners.docker]
    cpus = "2"
    disable_cache = false
    disable_entrypoint_overwrite = false
    image = "alvistack/gitlab-runner:latest"
    memory = "8192m"
    memory_swap = "8192m"
    oom_kill_disable = false
    privileged = false
    shm_size = 0
    tls_verify = false
    volumes = [
      "{{ gitlab_runner_runners_cache_dir }}:{{ gitlab_runner_runners_cache_dir }}",
      "/var/run/docker.sock:/var/run/docker.sock",
    ]
